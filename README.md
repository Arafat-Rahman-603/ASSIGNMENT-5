1. ver is a function scope variable declare method in javascript when we use ver for declare variable we can reassign and redeclare this variable , let is a block scope variable declare method in javascript when we use let for declare variable we can reassign  this variable but we can't redeclare this variable and const is a block scope variable declare method in javascript when we use const for declare variable we can't reassign and redeclare this variable.

2. In javascript spread operator use for expand array and object elements.

3. Map() return a new array, filter() return element by condition filtering and forEach() is in array for lopping or get one by one elements from array.

4. Arrow function is a short syntax function in modern javascript.

5. Template Literals is a modern way to write javascript string.